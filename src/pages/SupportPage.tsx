import { ArrowRight, Headphones, MessageCircle, PackageCheck, Settings2 } from 'lucide-react'
import { Link } from 'react-router-dom'

const supportOptions = [{ icon: Settings2, title: 'Get support', copy: 'Have a question? We’re here to help.', link: 'Start a conversation' }, { icon: PackageCheck, title: 'Order support', copy: 'Track a recent order or manage a return.', link: 'Check your order' }, { icon: Headphones, title: 'AppleCare+', copy: 'Expert support and hardware coverage.', link: 'Learn about AppleCare+' }, { icon: MessageCircle, title: 'Community', copy: 'Find answers from other Apple users.', link: 'Visit the community' }]

export default function SupportPage() { return <main className="support-page content-section"><div className="support-heading"><p className="eyebrow">We’re here for you</p><h1>How can we<br /><em>help?</em></h1><div className="support-search">Search support topics <span>⌘ K</span></div></div><div className="support-grid">{supportOptions.map(({ icon: Icon, title, copy, link }) => <article key={title}><div className="support-icon"><Icon size={20} /></div><h3>{title}</h3><p>{copy}</p><a href="#support">{link}<ArrowRight size={15} /></a></article>)}</div><div className="support-banner"><div><p className="eyebrow">Need something specific?</p><h2>Explore all support topics.</h2></div><Link className="button button-light" to="/store">Browse products <ArrowRight size={16} /></Link></div></main> }
